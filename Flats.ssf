#
# Builds flats using synthetic bias
#

cd flats
convert flat -out=../process
cd ../process

# calibrate flat using our computed synthetic bias
calibrate flat -bias="=64*$OFFSET"
stack pp_flat rej 3 3 -norm=mul -out=../masters/flat_stacked
